# 파이썬 실력의 기준

파이썬 실력은 크게 네 축으로 볼 수 있다.

| 축 | 핵심 질문 |
|---|---|
| 문법·표현 | 파이썬 문법으로 명확하고 간결하게 표현할 수 있는가 |
| 객체·실행 모델 | 코드가 실제로 어떻게 평가·참조·호출되는지 예측하는가 |
| 표준 라이브러리·생태계 | 직접 만들 필요 없는 것을 적절한 도구로 해결하는가 |
| 성능·동시성 | 파이썬의 비용과 한계를 이해하고 선택하는가 |

## 수준별 구분

| 수준 | 가능한 일 | 주로 부족한 부분 |
|---|---|---|
| 초급 | 문법으로 기능을 구현한다 | 코드 동작의 미묘한 차이를 예측하기 어렵다 |
| 중급 | 관용적인 코드로 기능을 구조화하고, 흔한 버그와 비용을 피한다 | 런타임·동시성·성능 문제의 근본 원인 판단이 제한적이다 |
| 고급 | 파이썬의 내부 특성과 한계를 감안해 적절한 설계를 선택한다 | 언어를 넘어 시스템·도메인 판단의 영역으로 간다 |

## 초급

알아야 할 것:

- 기본 문법: 제어문, 함수, 클래스, 예외
- 내장 자료형: `list`, `tuple`, `dict`, `set`, `str`
- 파일 처리, JSON, 간단한 HTTP/API 호출
- 패키지 설치와 가상환경의 기본 사용

특징:

- 기능을 구현할 수 있다.
- 문제를 풀기 위해 반복문과 조건문을 주로 사용한다.
- 코드가 왜 특정 방식으로 동작하는지보다 “일단 작동하는가”에 집중한다.

## 중급

알아야 할 것:

- 이름 바인딩과 객체 참조: `is`와 `==`, mutable/immutable
- 얕은 복사·깊은 복사, 가변 기본 인자 문제
- scope, closure, decorator
- iterator, iterable, generator, `yield`
- comprehension과 표준 라이브러리: `collections`, `itertools`, `functools`, `pathlib`, `contextlib`
- context manager와 `with`
- dataclass, type hint, protocol의 실용적 활용
- 예외 설계와 예외 전파
- `dict`·`set`의 해시 구조 및 평균 시간 복잡도
- 테스트와 디버깅 도구의 기본 활용

특징:

- “작동하는 코드”보다 읽기 쉽고 수정하기 쉬운 코드를 만든다.
- 불필요한 클래스를 만들거나, 모든 것을 `try/except`로 감싸거나, 대량 데이터를 리스트에 전부 올리는 식의 실수를 피한다.
- 파이썬다운 표현을 선택한다. 예: 스트리밍 처리가 필요하면 generator를, 값 객체에는 `dataclass`를 검토한다.

## 고급

알아야 할 것:

- 데이터 모델: dunder method, `__repr__`, `__eq__`, `__hash__`, descriptor, MRO
- CPython의 실행 특성: reference counting, cyclic GC, 객체 생성 비용
- GIL의 의미와 한계
- I/O bound와 CPU bound 구분
- `asyncio`, thread, process의 선택 기준
- 성능 측정과 프로파일링: 추측이 아니라 측정으로 병목 확인
- 대규모 데이터 처리에서 iterator·배치·벡터화의 차이
- import system, packaging, dependency 관리의 구조
- C 확장/NumPy 등이 순수 파이썬 반복문보다 빠른 이유

특징:

- “이 코드는 느릴 것 같다”가 아니라, 왜 느린지와 어디를 측정할지를 안다.
- `asyncio`를 유행처럼 쓰지 않고, 네트워크 I/O 동시성에 적합한 경우에 선택한다.
- CPU 연산을 스레드로 병렬화해도 GIL 때문에 기대한 속도 향상이 없을 수 있음을 안다.
- 복잡한 metaclass나 decorator를 구현할 수 있지만, 유지보수 비용 때문에 일반 코드에서는 피할 줄도 안다.

## 진짜 실력을 가르는 주제

| 주제 | 초급 | 중급 | 고급 |
|---|---|---|---|
| 변수 | 값을 담는 상자 | 객체에 붙인 이름 | 바인딩과 참조의 결과를 예측 |
| 리스트 | 여러 값을 저장 | 복사·aliasing을 주의 | 메모리·순회 비용을 고려 |
| 함수 | 재사용 단위 | closure/decorator 활용 | 호출 비용·API 설계까지 고려 |
| 반복 | `for` 사용 | comprehension/generator 선택 | lazy evaluation과 메모리 흐름 설계 |
| 클래스 | 속성과 메서드 묶기 | dataclass·상속을 절제해 활용 | data model/MRO/descriptor 이해 |
| 병렬성 | 모름 또는 thread 사용 | async/thread/process 구분 | GIL·취소·자원 관리까지 설계 |
| 성능 | 감으로 수정 | 자료구조·알고리즘 개선 | profiler로 병목을 확인하고 개선 |

## 압축한 기준

- 초급: **파이썬으로 구현한다.**
- 중급: **파이썬 코드의 동작을 예측하고, 관용적이고 안전하게 구현한다.**
- 고급: **파이썬의 런타임 특성·한계·비용을 이해하고, 문제에 맞게 활용하거나 우회한다.**
